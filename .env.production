VITE_API_URL=https://lms-be-9teh.onrender.com/api
# NOTE: this file is used by BOTH the Vercel web build and the mobile app build.
# For emulator testing against a LOCAL backend, temporarily use:
#   VITE_API_URL=http://10.0.2.2:5000/api   (then revert before pushing)
